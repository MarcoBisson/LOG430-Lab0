version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lab0
    ports:
      - "8000:8000"
    volumes:
      - .:/app/app
    environment:
      - PYTHONUNBUFFERED=1
